
-- Decompiled with the Synapse X Luau decompiler.

local function u1(p1, p2, ...)
	if not p2 then
		error(debug.traceback(p1, (...)), 2);
	end;
	if coroutine.status(p1) ~= "dead" then
		error(debug.traceback(p1, "Attempted to yield inside changed event!"), 2);
	end;
	return ...;
end;
return function(p3, ...)
	local v1 = coroutine.create(p3);
	return u1(v1, coroutine.resume(v1, ...));
end;

