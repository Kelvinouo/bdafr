-- Decompiled with the Synapse X Luau decompiler.

local v1 = {
	AssertKeyIsValid = function(p1)
		assert(type(p1) == "string", "Expects string got " .. typeof(p1));
		if #p1 >= 128 then
			error("Key '" .. tostring(p1) .. "' exceeds maximum length of " .. 128, 3);
		end;
	end
};
local u1 = {};
function v1.WarnOnce(p2)
	local v2 = debug.traceback();
	if u1[v2] then
		return;
	end;
	u1[v2] = true;
	warn(p2);
end;
v1.MAX_EXPIRATION_SECONDS = 2592000;
return v1;
