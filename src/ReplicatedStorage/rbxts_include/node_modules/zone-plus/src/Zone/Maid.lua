
-- Decompiled with the Synapse X Luau decompiler.

local u1 = {
	ClassName = "Maid", 
	new = function()
		return setmetatable({
			_tasks = {}
		}, u1);
	end, 
	isMaid = function(p1)
		local v1 = false;
		if type(p1) == "table" then
			v1 = p1.ClassName == "Maid";
		end;
		return v1;
	end, 
	__index = function(p2, p3)
		if u1[p3] then
			return u1[p3];
		end;
		return p2._tasks[p3];
	end, 
	__newindex = function(p4, p5, p6)
		if u1[p5] ~= nil then
			error(("'%s' is reserved"):format(tostring(p5)), 2);
		end;
		local l___tasks__2 = p4._tasks;
		local v3 = l___tasks__2[p5];
		if v3 == p6 then
			return;
		end;
		l___tasks__2[p5] = p6;
		if v3 then
			if type(v3) == "function" then
				v3();
				return;
			end;
			if typeof(v3) == "RBXScriptConnection" then
				v3:Disconnect();
				return;
			end;
			if not v3.Destroy then
				if v3.destroy then
					v3:destroy();
				end;
				return;
			end;
		else
			return;
		end;
		v3:Destroy();
	end, 
	giveTask = function(p7, p8)
		if not p8 then
			error("Task cannot be false or nil", 2);
		end;
		local v4 = #p7._tasks + 1;
		p7[v4] = p8;
		if type(p8) == "table" and not p8.Destroy and not p8.destroy then
			warn("[Maid.GiveTask] - Gave table task without .Destroy\n\n" .. debug.traceback());
		end;
		return v4;
	end, 
	give = function(p9, p10)
		if type(p10) == "table" and p10.isAPromise then
			local v5, v6 = p9:givePromise(p10);
			_ = v5;
			return p10, v6;
		end;
		return p10, p9:giveTask(p10);
	end, 
	doCleaning = function(p11)
		local l___tasks__7 = p11._tasks;
		for v8, v9 in pairs(l___tasks__7) do
			if typeof(v9) == "RBXScriptConnection" then
				l___tasks__7[v8] = nil;
				v9:Disconnect();
			end;
		end;
		local v10, v11 = next(l___tasks__7);
		while v11 ~= nil do
			l___tasks__7[v10] = nil;
			if type(v11) == "function" then
				v11();
			elseif typeof(v11) == "RBXScriptConnection" then
				v11:Disconnect();
			elseif v11.Destroy then
				v11:Destroy();
			elseif v11.destroy then
				v11:destroy();
			end;
			local v12, v13 = next(l___tasks__7);
			v10 = v12;
			v11 = v13;		
		end;
	end
};
u1.destroy = u1.doCleaning;
u1.clean = u1.doCleaning;
return u1;

