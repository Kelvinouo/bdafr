
-- Decompiled with the Synapse X Luau decompiler.

local u1 = require(script.Parent.Symbol);
local u2 = {
	new = function(p1, p2)
		local v1 = u1.new(p1);
		local v2 = {};
		for v3, v4 in ipairs(p2) do
			v2[v4] = u1.new(v4, v1);
		end;
		local v5 = {};
		function v5.__index(p3, p4)
			if v2[p4] then
				return v2[p4];
			end;
			if u2[p4] then
				return u2[p4];
			end;
			error("Unknown " .. p1 .. ": " .. tostring(p4), 2);
		end;
		function v5.__newindex()
			error("Cannot add new " .. p1, 2);
		end;
		return setmetatable({
			_scope = v1
		}, v5);
	end, 
	Is = function(p5, p6)
		return u1.IsInScope(p6, p5._scope);
	end
};
return u2;

