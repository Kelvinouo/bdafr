-- Decompiled with the Synapse X Luau decompiler.

local v1 = {};
if game:GetService("RunService"):IsServer() then
	v1.KnitServer = require(script.Knit.KnitServer);
else
	script.Knit.KnitServer:Destroy();
	v1.KnitClient = require(script.Knit.KnitClient);
end;
v1.RemoteProperty = require(script.Knit.Util.Remote.RemoteProperty);
v1.RemoteSignal = require(script.Knit.Util.Remote.RemoteSignal);
v1.ClientRemoteProperty = require(script.Knit.Util.Remote.ClientRemoteProperty);
v1.ClientRemoteSignal = require(script.Knit.Util.Remote.ClientRemoteSignal);
v1.Component = require(script.Knit.Util.Component);
v1.Loader = require(script.Knit.Util.Loader);
v1.Maid = require(script.Knit.Util.Maid);
v1.Option = require(script.Knit.Util.Option);
v1.Promise = require(script.Knit.Util.Promise);
v1.Ser = require(script.Knit.Util.Ser);
v1.Signal = require(script.Knit.Util.Signal);
v1.Streamable = require(script.Knit.Util.Streamable);
v1.StreamableUtil = require(script.Knit.Util.StreamableUtil);
v1.Symbol = require(script.Knit.Util.Symbol);
v1.TableUtil = require(script.Knit.Util.TableUtil);
v1.Thread = require(script.Knit.Util.Thread);
return v1;
