-- Decompiled with the Synapse X Luau decompiler.

local u1 = require(script.Parent.Parent.util);
local u2 = require(script.Parent.useSprings);
return function(p1, p2, p3, p4)
	local u3 = type(p3) == "function";
	local v1, v2 = u2(p1, p2, p1.useMemo(function()
		if u3 then
			return p3;
		end;
		local v3 = table.create(p2);
		local v4 = 0;
		local v5, v6, v7 = ipairs(p3);
		while true do
			v5(v6, v7);
			if not v5 then
				break;
			end;
			v7 = v5;
			local v8 = u1.merge({
				delay = 0.1
			}, v6);
			v8.delay = v4;
			v4 = v4 + v8.delay;
			v3[v5] = v8;		
		end;
		return v3;
	end, p4 or { {} }), p4);
	local v9 = p1.useValue({});
	if not u3 then
		return v1;
	end;
	table.clear(v9.value);
	for v10, v11 in pairs(v2) do
		v9.value[v10] = v11;
	end;
	v9.value.start = function(p5)
		local u4 = 0;
		v2.start(function(p6)
			local v12 = u1.merge({
				delay = 0.1
			}, p5(p6));
			v12.delay = u4;
			u4 = u4 + v12.delay;
			return v12;
		end);
	end;
	return v1, v9.value;
end;
