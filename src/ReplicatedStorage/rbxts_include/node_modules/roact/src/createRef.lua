-- Decompiled with the Synapse X Luau decompiler.

local u1 = require(script.Parent.Binding);
return function()
	local v1, v2 = u1.create(nil);
	local v3 = {};
	local v4 = {};
	function v4.__index(p1, p2)
		if p2 ~= "current" then
			return v1[p2];
		end;
		return v1:getValue();
	end;
	function v4.__newindex(p3, p4, p5)
		if p4 == "current" then
			error("Cannot assign to the 'current' property of refs", 2);
		end;
		v1[p4] = p5;
	end;
	function v4.__tostring(p6)
		return ("RoactRef(%s)"):format(tostring(v1:getValue()));
	end;
	setmetatable(v3, v4);
	return v3;
end;
