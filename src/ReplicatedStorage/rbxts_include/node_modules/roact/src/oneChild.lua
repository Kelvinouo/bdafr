
-- Decompiled with the Synapse X Luau decompiler.

return function(p1)
	if not p1 then
		return nil;
	end;
	local v1, v2 = next(p1);
	if not v2 then
		return nil;
	end;
	if next(p1, v1) then
		error("Expected at most child, had more than one child.", 2);
	end;
	return v2;
end;

