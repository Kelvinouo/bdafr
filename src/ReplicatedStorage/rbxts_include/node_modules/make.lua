
-- Decompiled with the Synapse X Luau decompiler.

return function(p1, p2)
	local l__Children__1 = p2.Children;
	local v2 = Instance.new(p1);
	for v3, v4 in pairs(p2) do
		if v3 ~= "Children" and v3 ~= "Parent" then
			local v5 = v2[v3];
			if typeof(v5) == "RBXScriptSignal" then
				v5:Connect(v4);
			else
				v2[v3] = v4;
			end;
		end;
	end;
	if l__Children__1 then
		for v6, v7 in ipairs(l__Children__1) do
			v7.Parent = v2;
		end;
	end;
	v2.Parent = p2.Parent;
	return v2;
end;

