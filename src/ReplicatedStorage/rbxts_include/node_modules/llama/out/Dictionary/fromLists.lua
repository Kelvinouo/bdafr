-- Decompiled with the Synapse X Luau decompiler.

local v1 = require(script.Parent.Parent.t);
local u1 = v1.tuple(v1.table, v1.table);
return function(p1, p2)
	assert(u1(p1, p2));
	local v2 = #p1;
	assert(v2 == #p2, "lists must be same size");
	local v3 = {};
	for v4 = 1, v2 do
		v3[p1[v4]] = p2[v4];
	end;
	return v3;
end;
