-- Decompiled with the Synapse X Luau decompiler.

local v1 = require(script.Parent.Parent.t);
local u1 = v1.tuple(v1.table, v1.optional(v1.intersection(v1.integer, v1.numberMin(0))));
local function u2(p1, p2)
	assert(u1(p1));
	local v2 = {};
	local v3 = 1;
	for v4 = 1, #p1 do
		if type(p1[v4]) == "table" and (not p2 or p2 > 0) then
			local v5 = u2(p1[v4], p2 and p2 - 1);
			for v6 = 1, #v5 do
				v2[v3] = v5[v6];
				v3 = v3 + 1;
			end;
		else
			v2[v3] = p1[v4];
		end;
	end;
	return v2;
end;
return u2;
