-- Decompiled with the Synapse X Luau decompiler.

local v1 = {};
v1.__index = v1;
function v1.new(p1, p2)
	assert(p1 ~= nil, "Symbol ID cannot be nil");
	if p2 ~= nil then
		assert(v1.Is(p2), "Scope must be a Symbol or nil");
	end;
	return setmetatable({
		ClassName = "Symbol", 
		_id = p1, 
		_scope = p2
	}, v1);
end;
function v1.Is(p3)
	local v2 = false;
	if type(p3) == "table" then
		v2 = getmetatable(p3) == v1;
	end;
	return v2;
end;
function v1.IsInScope(p4, p5)
	return v1.Is(p4) and p4._scope == p5;
end;
function v1.__tostring(p6)
	return ("Symbol<%s>"):format(p6._id);
end;
return v1;
