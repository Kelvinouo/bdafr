-- Decompiled with the Synapse X Luau decompiler.

local v1 = _G[script];
local v2 = {};
for v3, v4 in pairs(v1.import(script, script, "client", "events") or {}) do
	v2[v3] = v4;
end;
for v5, v6 in pairs(v1.import(script, script, "client", "lobby-client") or {}) do
	v2[v5] = v6;
end;
for v7, v8 in pairs(v1.import(script, script, "client", "config", "lobby-client-config") or {}) do
	v2[v7] = v8;
end;
for v9, v10 in pairs(v1.import(script, script, "client", "controllers", "custom-match-controller") or {}) do
	v2[v9] = v10;
end;
for v11, v12 in pairs(v1.import(script, script, "client", "controllers", "lobby-client-controller") or {}) do
	v2[v11] = v12;
end;
for v13, v14 in pairs(v1.import(script, script, "client", "controllers", "lobby-queue-controller") or {}) do
	v2[v13] = v14;
end;
for v15, v16 in pairs(v1.import(script, script, "client", "controllers", "party-controller") or {}) do
	v2[v15] = v16;
end;
for v17, v18 in pairs(v1.import(script, script, "client", "controllers", "event-handlers", "party-handler-controller") or {}) do
	v2[v17] = v18;
end;
for v19, v20 in pairs(v1.import(script, script, "client", "ui", "core") or {}) do
	v2[v19] = v20;
end;
for v21, v22 in pairs(v1.import(script, script, "client", "ui", "lib", "client-party") or {}) do
	v2[v21] = v22;
end;
for v23, v24 in pairs(v1.import(script, script, "client", "ui", "lib", "notifications", "party-invite-notification-card") or {}) do
	v2[v23] = v24;
end;
for v25, v26 in pairs(v1.import(script, script, "client", "ui", "party-hud-ui", "party-hud-app") or {}) do
	v2[v25] = v26;
end;
for v27, v28 in pairs(v1.import(script, script, "client", "ui", "party-hud-ui", "party-hud-button") or {}) do
	v2[v27] = v28;
end;
for v29, v30 in pairs(v1.import(script, script, "client", "ui", "party-hud-ui", "party-member-list", "party-hud-member-list") or {}) do
	v2[v29] = v30;
end;
for v31, v32 in pairs(v1.import(script, script, "client", "ui", "party-hud-ui", "party-member-list", "party-hud-member") or {}) do
	v2[v31] = v32;
end;
for v33, v34 in pairs(v1.import(script, script, "client", "ui", "party-invites-ui", "party-incoming-invite-card") or {}) do
	v2[v33] = v34;
end;
for v35, v36 in pairs(v1.import(script, script, "client", "ui", "party-invites-ui", "party-invites-app.story") or {}) do
	v2[v35] = v36;
end;
for v37, v38 in pairs(v1.import(script, script, "client", "ui", "party-invites-ui", "party-invites-app") or {}) do
	v2[v37] = v38;
end;
for v39, v40 in pairs(v1.import(script, script, "client", "ui", "party-invites-ui", "reducers", "party-invites-reducer") or {}) do
	v2[v39] = v40;
end;
for v41, v42 in pairs(v1.import(script, script, "client", "ui", "party-ui", "party-app") or {}) do
	v2[v41] = v42;
end;
for v43, v44 in pairs(v1.import(script, script, "client", "ui", "party-ui", "party-card.story") or {}) do
	v2[v43] = v44;
end;
for v45, v46 in pairs(v1.import(script, script, "client", "ui", "party-ui", "party-card") or {}) do
	v2[v45] = v46;
end;
for v47, v48 in pairs(v1.import(script, script, "client", "ui", "party-ui", "components", "left", "party-left-side") or {}) do
	v2[v47] = v48;
end;
for v49, v50 in pairs(v1.import(script, script, "client", "ui", "party-ui", "components", "left", "party-search") or {}) do
	v2[v49] = v50;
end;
for v51, v52 in pairs(v1.import(script, script, "client", "ui", "party-ui", "components", "left", "party-suggested-player") or {}) do
	v2[v51] = v52;
end;
for v53, v54 in pairs(v1.import(script, script, "client", "ui", "party-ui", "components", "right", "leave-party-button") or {}) do
	v2[v53] = v54;
end;
for v55, v56 in pairs(v1.import(script, script, "client", "ui", "party-ui", "components", "right", "party-member-card") or {}) do
	v2[v55] = v56;
end;
for v57, v58 in pairs(v1.import(script, script, "client", "ui", "party-ui", "components", "right", "party-member-list") or {}) do
	v2[v57] = v58;
end;
for v59, v60 in pairs(v1.import(script, script, "client", "ui", "party-ui", "reducers", "party-reducer") or {}) do
	v2[v59] = v60;
end;
for v61, v62 in pairs(v1.import(script, script, "client", "ui", "types", "app-configuration") or {}) do
	v2[v61] = v62;
end;
for v63, v64 in pairs(v1.import(script, script, "client", "ui", "types", "app-props") or {}) do
	v2[v63] = v64;
end;
for v65, v66 in pairs(v1.import(script, script, "client", "ui", "types", "store") or {}) do
	v2[v65] = v66;
end;
for v67, v68 in pairs(v1.import(script, script, "server", "lobby-server") or {}) do
	v2[v67] = v68;
end;
for v69, v70 in pairs(v1.import(script, script, "server", "commands", "game-teleport-command") or {}) do
	v2[v69] = v70;
end;
for v71, v72 in pairs(v1.import(script, script, "server", "config", "lobby-server-config") or {}) do
	v2[v71] = v72;
end;
for v73, v74 in pairs(v1.import(script, script, "server", "events", "events") or {}) do
	v2[v73] = v74;
end;
for v75, v76 in pairs(v1.import(script, script, "server", "events", "sync-events", "party-leave-queue-event") or {}) do
	v2[v75] = v76;
end;
for v77, v78 in pairs(v1.import(script, script, "server", "events", "sync-events", "player-join-queue-event") or {}) do
	v2[v77] = v78;
end;
for v79, v80 in pairs(v1.import(script, script, "server", "lib", "party") or {}) do
	v2[v79] = v80;
end;
for v81, v82 in pairs(v1.import(script, script, "server", "services", "device-info-service") or {}) do
	v2[v81] = v82;
end;
for v83, v84 in pairs(v1.import(script, script, "server", "services", "game-teleport-service") or {}) do
	v2[v83] = v84;
end;
for v85, v86 in pairs(v1.import(script, script, "server", "services", "lobby-server-service") or {}) do
	v2[v85] = v86;
end;
for v87, v88 in pairs(v1.import(script, script, "server", "services", "matchmaking-service") or {}) do
	v2[v87] = v88;
end;
for v89, v90 in pairs(v1.import(script, script, "server", "services", "party-service") or {}) do
	v2[v89] = v90;
end;
for v91, v92 in pairs(v1.import(script, script, "server", "services", "player-location-service") or {}) do
	v2[v91] = v92;
end;
for v93, v94 in pairs(v1.import(script, script, "server", "services", "playfab-service") or {}) do
	v2[v93] = v94;
end;
for v95, v96 in pairs(v1.import(script, script, "server", "services", "queue-service") or {}) do
	v2[v95] = v96;
end;
for v97, v98 in pairs(v1.import(script, script, "server", "services", "custom-match", "custom-match-manager-service") or {}) do
	v2[v97] = v98;
end;
for v99, v100 in pairs(v1.import(script, script, "server", "services", "device-info", "data", "continents") or {}) do
	v2[v99] = v100;
end;
for v101, v102 in pairs(v1.import(script, script, "server", "services", "event-handlers", "device-info-event-handler-service") or {}) do
	v2[v101] = v102;
end;
for v103, v104 in pairs(v1.import(script, script, "server", "services", "event-handlers", "party-event-handler-service") or {}) do
	v2[v103] = v104;
end;
for v105, v106 in pairs(v1.import(script, script, "server", "services", "event-handlers", "queue-event-handler-service") or {}) do
	v2[v105] = v106;
end;
for v107, v108 in pairs(v1.import(script, script, "shared", "lobby-core") or {}) do
	v2[v107] = v108;
end;
for v109, v110 in pairs(v1.import(script, script, "shared", "lobby-networking") or {}) do
	v2[v109] = v110;
end;
for v111, v112 in pairs(v1.import(script, script, "shared", "team-config") or {}) do
	v2[v111] = v112;
end;
for v113, v114 in pairs(v1.import(script, script, "shared", "custom-match", "custom-match-types") or {}) do
	v2[v113] = v114;
end;
for v115, v116 in pairs(v1.import(script, script, "shared", "device-info", "events") or {}) do
	v2[v115] = v116;
end;
for v117, v118 in pairs(v1.import(script, script, "shared", "event", "lobby-events") or {}) do
	v2[v117] = v118;
end;
for v119, v120 in pairs(v1.import(script, script, "shared", "party", "events") or {}) do
	v2[v119] = v120;
end;
for v121, v122 in pairs(v1.import(script, script, "shared", "party", "incoming-party-invite-dto") or {}) do
	v2[v121] = v122;
end;
for v123, v124 in pairs(v1.import(script, script, "shared", "party", "party-dto") or {}) do
	v2[v123] = v124;
end;
for v125, v126 in pairs(v1.import(script, script, "shared", "party", "party-invite-notification") or {}) do
	v2[v125] = v126;
end;
for v127, v128 in pairs(v1.import(script, script, "shared", "party", "party-queue-data") or {}) do
	v2[v127] = v128;
end;
for v129, v130 in pairs(v1.import(script, script, "shared", "party", "party-role") or {}) do
	v2[v129] = v130;
end;
for v131, v132 in pairs(v1.import(script, script, "shared", "party", "party") or {}) do
	v2[v131] = v132;
end;
for v133, v134 in pairs(v1.import(script, script, "shared", "party", "queue-state") or {}) do
	v2[v133] = v134;
end;
for v135, v136 in pairs(v1.import(script, script, "shared", "queue", "events") or {}) do
	v2[v135] = v136;
end;
for v137, v138 in pairs(v1.import(script, script, "shared", "queue", "queue-metadata") or {}) do
	v2[v137] = v138;
end;
for v139, v140 in pairs(v1.import(script, script, "shared", "sound", "sound-configuration") or {}) do
	v2[v139] = v140;
end;
for v141, v142 in pairs(v1.import(script, script, "shared", "teleport", "game-teleport-data") or {}) do
	v2[v141] = v142;
end;
for v143, v144 in pairs(v1.import(script, script, "types", "config-types") or {}) do
	v2[v143] = v144;
end;
for v145, v146 in pairs(v1.import(script, script, "types", "device-info-types") or {}) do
	v2[v145] = v146;
end;
for v147, v148 in pairs(v1.import(script, script, "types", "flamework") or {}) do
	v2[v147] = v148;
end;
for v149, v150 in pairs(v1.import(script, script, "types", "generic-types") or {}) do
	v2[v149] = v150;
end;
return v2;
