-- Decompiled with the Synapse X Luau decompiler.

local v1 = _G[script];
local v2 = {};
for v3, v4 in pairs(v1.import(script, script, "async-events", "async-event") or {}) do
	v2[v3] = v4;
end;
for v5, v6 in pairs(v1.import(script, script, "event-template") or {}) do
	v2[v5] = v6;
end;
for v7, v8 in pairs(v1.import(script, script, "events", "cancellable-event") or {}) do
	v2[v7] = v8;
end;
for v9, v10 in pairs(v1.import(script, script, "events", "parameterized-event") or {}) do
	v2[v9] = v10;
end;
for v11, v12 in pairs(v1.import(script, script, "sync-event-priority") or {}) do
	v2[v11] = v12;
end;
for v13, v14 in pairs(v1.import(script, script, "sync-events", "parameterized-sync-event") or {}) do
	v2[v13] = v14;
end;
for v15, v16 in pairs(v1.import(script, script, "sync-events", "sync-event") or {}) do
	v2[v15] = v16;
end;
return v2;
