
-- Decompiled with the Synapse X Luau decompiler.

local v1 = _G[script];
local v2 = {};
local l__Players__1 = v1.import(script, v1.getModule(script, "@rbxts", "services")).Players;
function v2.getNearbyPlayers(p1, p2, p3, p4)
	if p4 == nil then
		p4 = true;
	end;
	local v3 = {};
	local v4 = l__Players__1:GetPlayers();
	local function v5(p5)
		local v6 = table.find(p3 or {}, p5) ~= nil;
		if p4 and v6 then
			return nil;
		end;
		if not p4 and not v6 then
			return nil;
		end;
		local v7 = p5.Character;
		if v7 ~= nil then
			v7 = v7.PrimaryPart;
		end;
		if v7 and (p5.Character.PrimaryPart.Position - p1).Magnitude <= p2 then
			table.insert(v3, p5);
		end;
	end;
	for v8, v9 in ipairs(v4) do
		v5(v9, v8 - 1, v4);
	end;
	return v3;
end;
local u2 = Random.new();
function v2.randomUnitVector(p6, p7)
	return (CFrame.lookAt(Vector3.new(), p6) * CFrame.Angles(0, 0, u2:NextNumber(0, 2 * math.pi)) * CFrame.Angles(math.acos(u2:NextNumber(math.cos(p7), 1)), 0, 0)).LookVector;
end;
return {
	WorldUtil = v2
};

