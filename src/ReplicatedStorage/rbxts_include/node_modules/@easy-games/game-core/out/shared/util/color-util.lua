
-- Decompiled with the Synapse X Luau decompiler.

local v1 = {
	hexColor = function(p1)
		return Color3.fromRGB(bit32.band(bit32.rshift(p1, 16), 255), bit32.band(bit32.rshift(p1, 8), 255), bit32.band(p1, 255));
	end, 
	richTextColor = function(p2)
		return "rgb(" .. tostring(math.round(p2.R * 255)) .. "," .. tostring(math.round(p2.G * 255)) .. "," .. tostring(math.round(p2.B * 255)) .. ")";
	end, 
	darken = function(p3, p4)
		return Color3.new(p3.R * p4, p3.G * p4, p3.B * p4);
	end, 
	brighten = function(p5, p6)
		return Color3.new(p6 + (1 - p6) * p5.R, p6 + (1 - p6) * p5.G, p6 + (1 - p6) * p5.B);
	end
};
v1.BLACK = v1.hexColor(0);
v1.WHITE = v1.hexColor(16777215);
return {
	ColorUtil = v1
};

