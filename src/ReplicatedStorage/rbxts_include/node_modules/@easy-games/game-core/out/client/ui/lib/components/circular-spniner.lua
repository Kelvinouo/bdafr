-- Decompiled with the Synapse X Luau decompiler.

local v1 = _G[script];
local u1 = v1.import(script, v1.getModule(script, "@rbxts", "roact").src);
local l__TweenService__2 = v1.import(script, v1.getModule(script, "@rbxts", "services")).TweenService;
local l__ImageId__3 = v1.import(script, script.Parent.Parent.Parent.Parent.Parent, "shared", "images", "image-id").ImageId;
return {
	CircularSpinner = v1.import(script, v1.getModule(script, "@rbxts", "roact-hooks").src).new(u1)(function(p1, p2)
		local u4 = u1.createRef();
		p2.useEffect(function()
			local v2 = u4:getValue();
			if v2 then
				l__TweenService__2:Create(v2, TweenInfo.new(4, Enum.EasingStyle.Linear, Enum.EasingDirection.In, -1), {
					Rotation = 360
				}):Play();
			end;
		end, {});
		local v3 = {};
		for v4, v5 in pairs(p1) do
			v3[v4] = v5;
		end;
		local v6 = {
			ZIndex = 1000, 
			Image = l__ImageId__3.LOADING_SPINNER, 
			BackgroundTransparency = 1, 
			Size = UDim2.fromScale(1, 1), 
			Position = UDim2.fromScale(0.5, 0.5), 
			AnchorPoint = Vector2.new(0.5, 0.5), 
			[u1.Ref] = u4
		};
		for v7, v8 in pairs(v3) do
			v6[v7] = v8;
		end;
		return u1.createElement("ImageLabel", v6);
	end)
};
