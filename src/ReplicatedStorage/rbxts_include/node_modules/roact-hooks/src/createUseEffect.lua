-- Decompiled with the Synapse X Luau decompiler.

return function(p1)
	return function(p2, p3)
		assert(typeof(p2) == "function", "useEffect callback is not a function");
		p1.hookCounter = p1.hookCounter + 1;
		p1.effects[p1.hookCounter] = { p2, p3 };
	end;
end;
