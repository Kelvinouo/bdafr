-- Decompiled with the Synapse X Luau decompiler.

local v1 = require(script.Parent.Parent.Runtime);
local u1 = require(script.Parent.Parent.Style);
local u2 = require(script.Parent.Parent.create);
return v1.widget(function(p1)
	local v2, v3 = v1.useState(false);
	v1.useInstance(function(p2)
		local v4 = u1.get();
		return u2("TextButton", {
			[p2] = "button", 
			BackgroundColor3 = v4.bg3, 
			BorderSizePixel = 0, 
			Font = Enum.Font.SourceSans, 
			Size = UDim2.new(0, 100, 0, 40), 
			TextColor3 = v4.textColor, 
			AutomaticSize = Enum.AutomaticSize.X, 
			TextSize = 21, 
			Activated = function()
				v3(true);
			end,
			u2("UIPadding", {
				PaddingLeft = UDim.new(0, 10), 
				PaddingRight = UDim.new(0, 10)
			}), (u2("UICorner"))
		});
	end).button.Text = p1;
	local v5 = {};
	function v5.clicked()
		if not v2 then
			return false;
		end;
		v3(false);
		return true;
	end;
	return v5;
end);
