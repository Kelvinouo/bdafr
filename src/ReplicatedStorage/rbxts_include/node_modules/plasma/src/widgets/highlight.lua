-- Decompiled with the Synapse X Luau decompiler.

local v1 = require(script.Parent.Parent.Runtime);
local u1 = require(script.Parent.Parent.create);
return v1.widget(function(p1, p2)
	p2 = p2 or {};
	local v2 = v1.useInstance(function(p3)
		return u1("Highlight", {
			[p3] = "highlight"
		});
	end);
	v2.highlight.Adornee = p1;
	v1.useEffect(function()
		v2.highlight.OutlineColor = p2.outlineColor or Color3.new(1, 1, 1);
		v2.highlight.FillColor = p2.fillColor or Color3.new(1, 0, 0);
	end, p2.fillColor, p2.outlineColor);
	v2.highlight.FillTransparency = p2.fillTransparency and 0.5;
	v2.highlight.OutlineTransparency = p2.outlineTransparency and 0;
	v2.highlight.DepthMode = p2.depthMode or Enum.HighlightDepthMode.AlwaysOnTop;
end);
