-- Decompiled with the Synapse X Luau decompiler.

local v1 = require(script.Parent.Parent.Runtime);
local u1 = require(script.Parent.Parent.automaticSize);
return v1.widget(function(p1, p2)
	if type(p1) == "function" and p2 == nil then
		p2 = p1;
		p1 = {};
	end;
	if p1.padding then
		if type(p1.padding) == "number" then
			p1.padding = UDim.new(0, p1.padding);
		end;
	else
		p1.padding = UDim.new(0, 10);
	end;
	v1.useInstance(function(p3)
		local v2 = Instance.new("Frame");
		v2.BackgroundTransparency = 1;
		local v3 = Instance.new("UIListLayout");
		v3.SortOrder = Enum.SortOrder.LayoutOrder;
		v3.FillDirection = Enum.FillDirection.Horizontal;
		v3.Padding = p1.padding;
		v3.Parent = v2;
		p3.frame = v2;
		u1(v2);
		return v2;
	end).frame.UIListLayout.HorizontalAlignment = p1.alignment or Enum.HorizontalAlignment.Left;
	v1.scope(p2);
end);
