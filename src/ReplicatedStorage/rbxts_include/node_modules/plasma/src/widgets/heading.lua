-- Decompiled with the Synapse X Luau decompiler.

local v1 = require(script.Parent.Parent.Runtime);
local u1 = require(script.Parent.Parent.Style);
local u2 = require(script.Parent.Parent.create);
return v1.widget(function(p1, p2)
	p2 = p2 or {};
	local l__heading__2 = v1.useInstance(function(p3)
		return u2("TextLabel", {
			[p3] = "heading", 
			BackgroundTransparency = 1, 
			Font = Enum.Font.GothamBold, 
			AutomaticSize = Enum.AutomaticSize.XY, 
			TextColor3 = u1.get().mutedTextColor, 
			TextSize = 20, 
			RichText = true
		});
	end).heading;
	l__heading__2.Text = p1;
	l__heading__2.Font = p2.font or Enum.Font.GothamBold;
end);
