-- Decompiled with the Synapse X Luau decompiler.

local v1 = require(script.Parent.Parent.Runtime);
local u1 = require(script.Parent.Parent.create);
return v1.widget(function(p1, p2)
	p2 = p2 or {};
	local v2, v3 = v1.useState(false);
	local v4, v5 = v1.useState(false);
	local l__checkbox__6 = v1.useInstance(function(p3)
		return u1("Frame", {
			[p3] = "checkbox", 
			BackgroundTransparency = 1, 
			Name = "Checkbox", 
			Size = UDim2.new(0, 30, 0, 30), 
			AutomaticSize = Enum.AutomaticSize.X,
			u1("TextButton", {
				BackgroundColor3 = Color3.fromRGB(54, 54, 54), 
				BorderSizePixel = 0, 
				Font = Enum.Font.SourceSansBold, 
				Size = UDim2.new(0, 30, 0, 30), 
				TextColor3 = Color3.fromRGB(153, 153, 153), 
				TextSize = 24, 
				Activated = function()
					v5(true);
					v3(function(p4)
						return not p4;
					end);
				end,
				(u1("UICorner", {
					CornerRadius = UDim.new(0, 8)
				}))
			}), u1("TextLabel", {
				BackgroundColor3 = Color3.fromRGB(255, 255, 255), 
				Font = Enum.Font.GothamMedium, 
				TextColor3 = Color3.fromRGB(203, 203, 203), 
				TextSize = 18, 
				AutomaticSize = Enum.AutomaticSize.X, 
				RichText = true
			}), u1("UIListLayout", {
				FillDirection = Enum.FillDirection.Horizontal, 
				Padding = UDim.new(0, 10), 
				SortOrder = Enum.SortOrder.LayoutOrder, 
				VerticalAlignment = Enum.VerticalAlignment.Center
			})
		});
	end).checkbox;
	l__checkbox__6.TextLabel.Text = p1;
	l__checkbox__6.TextButton.AutoButtonColor = not p2.disabled;
	v1.useEffect(function()
		if p2.checked ~= nil then
			local v7 = p2.checked;
		else
			v7 = v2;
		end;
		if v7 then
			local v8 = "\226\156\147";
		else
			v8 = "";
		end;
		l__checkbox__6.TextButton.Text = v8;
	end, p2.checked, v2);
	v1.useEffect(function()
		l__checkbox__6.TextButton.BackgroundColor3 = p2.disabled and Color3.fromRGB(112, 112, 112) or Color3.fromRGB(54, 54, 54);
	end, p2.disabled);
	local v9 = {
		checked = function()
			if not p2.checked and not v2 then
				return false;
			end;
			return true;
		end
	};
	function v9.clicked()
		if not v4 then
			return false;
		end;
		v5(false);
		return true;
	end;
	return v9;
end);
