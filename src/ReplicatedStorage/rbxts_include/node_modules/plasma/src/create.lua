-- Decompiled with the Synapse X Luau decompiler.

local u1 = require(script.Parent.Runtime);
return function(p1, p2)
	p2 = p2 or {};
	local v1 = u1.useEventCallback();
	local v2 = Instance.new(p1);
	for v3, v4 in pairs(p2) do
		if type(v4) == "function" then
			if v1 then
				v1(v2, v3, v4);
			else
				v2[v3]:Connect(v4);
			end;
		elseif type(v3) == "number" then
			v4.Parent = v2;
		elseif type(v3) == "table" then
			v3[v4] = v2;
			if p2.Name == nil then
				v2.Name = v4;
			end;
		else
			v2[v3] = v4;
		end;
	end;
	return v2;
end;
