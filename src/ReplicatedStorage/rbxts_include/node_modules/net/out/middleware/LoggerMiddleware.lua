-- Decompiled with the Synapse X Luau decompiler.

local u1 = game:GetService("RunService"):IsStudio();
local l__HttpService__2 = game:GetService("HttpService");
local function u3(p1, p2)
	if u1 then
		local v1 = {};
		for v2, v3 in ipairs(p2) do
			table.insert(v1, l__HttpService__2:JSONEncode(v3));
		end;
		print("[rbx-net] Called", p1, "with arguments", "[ " .. table.concat(v1, ", ") .. " ]");
	end;
end;
return function(p3)
	p3 = p3 or {};
	local u4 = p3.Logger or u3;
	return function(p4, p5)
		local u5 = p3.Name or p5:GetInstance().Name;
		return function(p6, ...)
			u4(u5, { ... });
			return p4(p6, ...);
		end;
	end;
end;
